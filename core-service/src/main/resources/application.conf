ktor {
  deployment {
    port = 8080
    port = ${?CORE_PORT}
    host = "0.0.0.0"
  }
  application {
    modules = [ com.stockyard.core.ApplicationKt.module ]
  }
}

stockyard {
  postgres {
    host     = "postgres"
    host     = ${?PG_HOST}
    port     = 5432
    port     = ${?PG_PORT}
    db       = "stockyard"
    db       = ${?PG_DB}
    user     = "stockyard"
    user     = ${?PG_USER}
    password = ""
    password = ${?PG_PASSWORD}
  }
  redis {
    url      = "redis://redis:6379"
    url      = ${?REDIS_URL}
    password = ""
    password = ${?REDIS_PASSWORD}
  }
  clickhouse {
    host     = "clickhouse"
    host     = ${?CH_HOST}
    port     = 8123
    port     = ${?CH_PORT}
    db       = "stockyard"
    db       = ${?CH_DB}
    user     = "stockyard"
    user     = ${?CH_USER}
    password = ""
    password = ${?CH_PASSWORD}
  }
  argon2 {
    pepper = ""
    pepper = ${?ARGON2_PEPPER}
  }
  otel {
    serviceName  = "core-service"
    serviceName  = ${?OTEL_SERVICE_NAME}
    otlpEndpoint = "http://otel-collector:4317"
    otlpEndpoint = ${?OTEL_EXPORTER_OTLP_ENDPOINT}
  }

  # ADR-010: source of quotes data.
  #   fixture — Core publishes synthetic ticks (dev / macOS without driver)
  #   driver  — Quotes Service is the single writer (Linux + /dev/stockyard)
  quotesSource = "fixture"
  quotesSource = ${?STOCKYARD_QUOTES_SOURCE}

  devFixture {
    intervalSec    = 1
    intervalSec    = ${?STOCKYARD_FIXTURE_INTERVAL_SEC}
    jitterPercent  = 0.4
    jitterPercent  = ${?STOCKYARD_FIXTURE_JITTER_PCT}
  }
}
